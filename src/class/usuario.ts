import { ObjectID } from "mongodb";

export class Retangulo {
  id: ObjectID;
  altura: number;
  largura: number;
  // constructor(id: ObjectID, altura: number, largura: number) {
  //   this.altura = altura;
  //   this.largura = largura;
  // }
}
